/** Client-safe featured-assets list. Re-exported so client components don't
 * need to import the router (which pulls in server-only Binance/CoinGecko/OpenSea helpers). */

export { FEATURED } from "./registry";
